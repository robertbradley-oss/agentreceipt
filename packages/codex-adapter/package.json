{
  "name": "@agentreceipt/codex-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Privacy-first adapter for documented Codex exec JSONL events.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/src/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/adapter.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.10.0"
  }
}
