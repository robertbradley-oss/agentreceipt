{
  "name": "@agentreceipt/runback",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Task-agnostic component catalog and deterministic preflight rail planner.",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/runback.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.10.0"
  }
}
