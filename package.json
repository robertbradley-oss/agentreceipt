{
  "name": "agentreceipt",
  "version": "0.0.0",
  "private": true,
  "description": "Portable, verifiable proof-of-work receipts for AI coding agents.",
  "license": "MIT",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "check": "pnpm --filter @agentreceipt/schema build && pnpm --filter @agentreceipt/runback build && pnpm -r --if-present check",
    "test": "pnpm run build && pnpm -r --if-present test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
